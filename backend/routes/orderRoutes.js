import {Router} from "express";import * as c from "../controllers/orderController.js";import {protect,adminOnly,optionalAuth} from "../middleware/auth.js";const r=Router();r.get("/track/:query",c.trackOrder);r.post("/quote",c.getOrderQuote);r.post("/",optionalAuth,c.createOrder);r.use(protect);r.get("/my",c.getMyOrders);r.get("/my/:id/invoice",c.getMyInvoice);r.patch("/my/:id",c.customerUpdateOrder);r.get("/",adminOnly,c.getOrders);r.get("/:id/invoice",adminOnly,c.getInvoice);r.patch("/:id",adminOnly,c.updateOrder);export default r;
